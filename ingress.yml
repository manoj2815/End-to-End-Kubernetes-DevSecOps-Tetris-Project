apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tetris-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tetris-blue
            port:
              number: 80
